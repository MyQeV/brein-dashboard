/** Control heights: 36/44px on touch widths, the desktop sizes from lg up. Buttons and fields must agree or a row misaligns. */
export const CONTROL_HEIGHT = {
  sm: "h-9 lg:h-8",
  md: "h-11 lg:h-10",
} as const;
